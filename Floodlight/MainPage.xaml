<Page
    x:Class="Floodlight.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Floodlight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <GridView Grid.Row="0" x:Name="ImagesAvailable">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="10">
                        <Image Source="{Binding image.location}" Width="{Binding image.width}" Height="{Binding image.height}"></Image>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <CommandBar Grid.Row="1">
            <AppBarButton Name="Download" Icon="Download" Label="Update Images" Click="Download_OnClick" />
            <AppBarButton Name="Change" Icon="Sync" Label="Change Background" Click="Change_OnClick" />
            <AppBarButton Name="Settings" Icon="Setting" Label="Settings" Click="Settings_OnClick" />
        </CommandBar>
    </Grid>
</Page>
